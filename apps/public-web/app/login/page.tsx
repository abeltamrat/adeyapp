export default function LoginPage() {
  return (
    <main className="section-card">
      <p className="eyebrow">Login</p>
      <h1 className="page-title">Authentication entry</h1>
      <p className="muted">This will later hand off to the shared auth flow.</p>
    </main>
  );
}
