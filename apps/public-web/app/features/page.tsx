export default function FeaturesPage() {
  return (
    <main className="section-card">
      <p className="eyebrow">Features</p>
      <h1 className="page-title">Role-based spa operations</h1>
      <p className="muted">
        Public marketing, superadmin controls, branch operations, customer booking, and
        employee attendance all live in the same product family.
      </p>
    </main>
  );
}
